---
phase: 02-team-match-visuals
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/pages/Team.tsx
  - frontend/src/pages/Team.module.css
  - frontend/src/components/team/TeamHeader.tsx
  - frontend/src/components/team/TeamHeader.module.css
  - frontend/src/components/team/TeamCloset.tsx
  - frontend/src/components/team/TeamCloset.module.css
  - frontend/src/components/merch/ProductGrid.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - TEAM-01
  - TEAM-03

must_haves:
  truths:
    - "User can navigate to /team/:slug and see a team page for any of the 48 teams"
    - "Team page displays interactive 3D pixel logo at the top with fluid sim behind it"
    - "Team page displays merch collection filtered by team below the logo"
    - "Team page shows team name, group, and flag in terminal aesthetic"
    - "ProductGrid accepts optional team filter prop for filtering merch by collection"
  artifacts:
    - path: "frontend/src/pages/Team.tsx"
      provides: "Team page route component"
      exports: ["Team"]
    - path: "frontend/src/components/team/TeamHeader.tsx"
      provides: "PixelGrid + team name/group/flag display"
      exports: ["TeamHeader"]
    - path: "frontend/src/components/team/TeamCloset.tsx"
      provides: "Team-filtered merch collection"
      exports: ["TeamCloset"]
  key_links:
    - from: "frontend/src/pages/Team.tsx"
      to: "frontend/src/data/teams.ts"
      via: "getTeamBySlug lookup from URL param"
      pattern: "getTeamBySlug"
    - from: "frontend/src/components/team/TeamHeader.tsx"
      to: "frontend/src/components/grid/PixelGrid.tsx"
      via: "renders PixelGrid with team logo pixels"
      pattern: "<PixelGrid.*logoPixels"
    - from: "frontend/src/components/team/TeamCloset.tsx"
      to: "frontend/src/lib/shopify.ts"
      via: "fetches products filtered by team collection handle"
      pattern: "fetchProducts.*collection"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Team.tsx"
      via: "React Router route /team/:slug"
      pattern: 'path="/team/:slug"'
---

<objective>
Build team pages with interactive 3D pixel logo at top and team-filtered merch collection below. Add /team/:slug route and team navigation.

Purpose: Each of the 48 teams gets a dedicated page combining the visual 3D experience with commerce -- the core differentiator of ten+1.
Output: Team page, TeamHeader (with PixelGrid), TeamCloset (filtered merch), updated routing.
</objective>

<execution_context>
@C:/Users/nofun/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nofun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-team-match-visuals/02-RESEARCH.md
@.planning/phases/02-team-match-visuals/02-01-SUMMARY.md

<interfaces>
From frontend/src/components/grid/PixelGrid.tsx (created in 02-01):
```typescript
interface PixelGridProps {
  logoPixels: number[];
  matchMode?: { awayPixels: number[]; switchInterval?: number; };
  height?: string;
  className?: string;
}
export function PixelGrid(props: PixelGridProps): JSX.Element;
```

From frontend/src/data/teams.ts (created in 02-01):
```typescript
export interface TeamData {
  name: string; slug: string; code: string; group: string;
  flag: string; primaryColor: [number, number, number];
}
export const TEAMS: TeamData[];
export function getTeamBySlug(slug: string): TeamData | undefined;
export function getTeamByName(name: string): TeamData | undefined;
export function getTeamsByGroup(group: string): TeamData[];
```

From frontend/src/data/team-logos/index.ts (created in 02-01):
```typescript
export function getLogoPixels(slug: string, fallbackLetter: string): number[];
```

From frontend/src/lib/shopify.ts (existing):
```typescript
export async function fetchProducts(): Promise<ShopifyProduct[]>;
// Currently fetches ALL products. Need to add optional collection filter.
```

From frontend/src/components/layout/Line.tsx (existing):
```typescript
export function Line({ n, children }: { n: number; children: React.ReactNode });
export function Blank({ n }: { n: number });
export function useLineCounter(start?: number): () => number;
```

From frontend/src/App.tsx (existing):
```typescript
// Routes: /, /matches, /groups, /slip, /merch, /merch/:handle
// Need to add: /team/:slug
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Team page with TeamHeader and TeamCloset components</name>
  <files>
    frontend/src/pages/Team.tsx,
    frontend/src/pages/Team.module.css,
    frontend/src/components/team/TeamHeader.tsx,
    frontend/src/components/team/TeamHeader.module.css,
    frontend/src/components/team/TeamCloset.tsx,
    frontend/src/components/team/TeamCloset.module.css
  </files>
  <read_first>
    C:/Users/nofun/tenplusone/frontend/src/App.tsx,
    C:/Users/nofun/tenplusone/frontend/src/pages/Merch.tsx,
    C:/Users/nofun/tenplusone/frontend/src/components/merch/ProductGrid.tsx,
    C:/Users/nofun/tenplusone/frontend/src/components/layout/Line.tsx,
    C:/Users/nofun/tenplusone/frontend/src/lib/shopify.ts,
    C:/Users/nofun/tenplusone/frontend/src/styles/tokens.css,
    C:/Users/nofun/tenplusone/frontend/src/data/teams.ts,
    C:/Users/nofun/tenplusone/frontend/src/data/team-logos/index.ts,
    C:/Users/nofun/tenplusone/frontend/src/components/grid/PixelGrid.tsx
  </read_first>
  <action>
    **TeamHeader.tsx**: Displays team identity above the 3D grid.
    - Props: `{ team: TeamData }`
    - Uses `useLineCounter` for terminal line numbers
    - Renders: `Line` with team flag + name + code (e.g., "// GERMANY [GER]" with flag emoji)
    - Renders: `Line` with group info (e.g., "// Group E")
    - Renders: `Blank` line
    - Renders: `PixelGrid` with `logoPixels={getLogoPixels(team.slug, team.name[0])}` and `height="50vh"`
    - CSS module: minimal, team name uses `var(--white)` color, group uses `var(--dim)`

    **TeamCloset.tsx**: Team-filtered merch collection.
    - Props: `{ teamSlug: string }`
    - Uses `useLineCounter` for terminal line numbers
    - Fetches products from Shopify filtered by team collection handle: `fetchProductsByCollection(teamSlug)`
    - If no Shopify token configured (mock mode), shows all mock products with a line: `// {team} collection`
    - Renders: section header Line `// TEAM CLOSET`
    - Maps products to `ProductCard` components (reuses existing component)
    - Loading and error states match existing ProductGrid patterns (dim comment style)
    - CSS module: grid layout for product cards (uses CSS custom properties from tokens.css)

    **Team.tsx**: Page component for /team/:slug route.
    - Uses `useParams<{ slug: string }>()` from react-router
    - Looks up team via `getTeamBySlug(slug)`
    - If team not found, renders `Line` with "// team not found" message
    - Renders `<TeamHeader team={team} />` then `<TeamCloset teamSlug={team.slug} />`
    - CSS module: page container spacing

    **Update shopify.ts**: Add `fetchProductsByCollection(collectionHandle: string)` function.
    - GraphQL query: `collection(handle: $handle) { products(first: 20) { edges { node { ...ProductFields } } } }`
    - In mock mode: return all mock products (no filtering available without real Shopify data)
    - Keep existing `fetchProducts()` unchanged for the main merch page
  </action>
  <verify>
    <automated>cd C:/Users/nofun/tenplusone/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/pages/Team.tsx` exists and exports `Team` or default export
    - File `frontend/src/components/team/TeamHeader.tsx` exports `TeamHeader`
    - File `frontend/src/components/team/TeamCloset.tsx` exports `TeamCloset`
    - `grep "getTeamBySlug" frontend/src/pages/Team.tsx` finds team lookup
    - `grep "PixelGrid" frontend/src/components/team/TeamHeader.tsx` finds grid usage
    - `grep "getLogoPixels" frontend/src/components/team/TeamHeader.tsx` finds logo lookup
    - `grep "fetchProductsByCollection\|fetchProducts" frontend/src/components/team/TeamCloset.tsx` finds product fetching
    - `grep "fetchProductsByCollection" frontend/src/lib/shopify.ts` finds new function
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>
    Team page renders TeamHeader (with PixelGrid showing team logo) and TeamCloset (merch filtered by team). All components use terminal aesthetic (Line, Blank, useLineCounter) and CSS Modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add team route and update navigation with team links</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/components/merch/ProductGrid.tsx,
    frontend/src/components/layout/Nav.tsx
  </files>
  <read_first>
    C:/Users/nofun/tenplusone/frontend/src/App.tsx,
    C:/Users/nofun/tenplusone/frontend/src/components/layout/Nav.tsx,
    C:/Users/nofun/tenplusone/frontend/src/components/merch/ProductGrid.tsx,
    C:/Users/nofun/tenplusone/frontend/src/pages/Team.tsx,
    C:/Users/nofun/tenplusone/frontend/src/components/matches/MatchList.tsx
  </read_first>
  <action>
    **App.tsx**: Add team page route.
    - Import Team page: `import { Team } from './pages/Team';` (or lazy import)
    - Add route: `<Route path="/team/:slug" element={<Team />} />`
    - Place it after existing routes but before any catch-all

    **Nav.tsx**: Add "teams" link to navigation.
    - Add a "teams" nav link that goes to `/groups` (teams are accessible via group view)
    - OR better: make team names in the match list clickable (handled in MatchList below)
    - Keep nav simple -- the existing "groups" link is the entry point to teams

    **ProductGrid.tsx**: Add optional team filter prop to support COMM-04 (merch catalog organized by team).
    - Add optional `teamFilter?: string` prop
    - When `teamFilter` is set, use `fetchProductsByCollection(teamFilter)` instead of `fetchProducts()`
    - When no filter, keep existing behavior (fetch all products)
    - This fixes the COMM-04 gap: merch can now be organized by team
    - Update the section header: if teamFilter, show `// {teamFilter.toUpperCase()} MERCH`, else keep `// MERCH`

    **MatchList.tsx**: Make team names in match rows link to team pages.
    - Wrap home and away team names with `<Link to={'/team/' + getTeamByName(match.h)?.slug}>` (if team exists)
    - Import `Link` from react-router and `getTeamByName` from data/teams
    - Style links: color inherit, text-decoration none, hover: var(--green) per FRNT-02
    - If team has no slug (e.g., "TBD" teams), render as plain text (no link)
  </action>
  <verify>
    <automated>cd C:/Users/nofun/tenplusone/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'path="/team/:slug"' frontend/src/App.tsx` finds the route
    - `grep "teamFilter" frontend/src/components/merch/ProductGrid.tsx` finds the prop
    - `grep "fetchProductsByCollection" frontend/src/components/merch/ProductGrid.tsx` finds conditional fetch
    - `grep "Link.*team/" frontend/src/components/matches/MatchList.tsx` finds team links in match list
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>
    /team/:slug route works in App.tsx. Team names in match list link to team pages. ProductGrid accepts optional teamFilter prop for organized-by-team merch browsing (COMM-04).
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with zero errors
- Route `/team/:slug` exists in App.tsx
- TeamHeader renders PixelGrid with logo from team-logos
- TeamCloset fetches team-filtered products
- Match list team names link to /team/:slug pages
- ProductGrid supports optional team filtering
</verification>

<success_criteria>
- Any of 48 team slugs resolves to a team page with 3D grid + merch
- PixelGrid shows the team's logo (real for 3 teams, placeholder letter for 45)
- Merch section shows team-filtered products (or all products in mock mode)
- Team names in match browser are clickable links to team pages
- Terminal aesthetic maintained (Line components, CSS tokens, Geist Mono)
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-match-visuals/02-02-SUMMARY.md`
</output>
