---
phase: 01-commerce-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/styles/tokens.css
  - frontend/src/styles/global.css
  - frontend/src/components/layout/Line.tsx
  - frontend/src/components/layout/Line.module.css
  - frontend/src/components/layout/Nav.tsx
  - frontend/src/components/layout/Nav.module.css
  - frontend/src/components/layout/Editor.tsx
  - frontend/src/components/layout/Editor.module.css
  - frontend/src/components/matches/MatchList.tsx
  - frontend/src/components/matches/MatchList.module.css
  - frontend/src/components/matches/OddsButton.tsx
  - frontend/src/components/slip/BetSlip.tsx
  - frontend/src/components/slip/BetSlip.module.css
  - frontend/src/stores/slip.ts
  - frontend/src/stores/matches.ts
  - frontend/src/data/matches.ts
  - frontend/src/data/groups.ts
  - frontend/src/pages/Matches.tsx
  - frontend/src/pages/Groups.tsx
  - frontend/src/pages/BetSlipPage.tsx
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
autonomous: true
requirements:
  - FRNT-01
  - FRNT-02
  - FRNT-03
  - FRNT-04
  - COMM-01
  - COMM-02

must_haves:
  truths:
    - "User sees match list with line numbers, Geist Mono font, and monochrome palette"
    - "User can click odds buttons to add/remove predictions to a bet slip"
    - "User can filter matches by group"
    - "User can navigate between matches, groups, and slip views via a sticky nav bar"
    - "Layout is responsive on mobile (560px breakpoint)"
    - "Green color appears only on hover states, never as default"
  artifacts:
    - path: "frontend/src/components/layout/Line.tsx"
      provides: "Terminal line-number component"
      exports: ["Line", "Blank", "useLineCounter"]
    - path: "frontend/src/components/layout/Nav.tsx"
      provides: "Sticky navigation bar"
      exports: ["Nav"]
    - path: "frontend/src/stores/slip.ts"
      provides: "Bet slip state management"
      exports: ["useSlipStore"]
    - path: "frontend/src/components/matches/MatchList.tsx"
      provides: "Match browser with odds"
      exports: ["MatchList"]
    - path: "frontend/src/styles/tokens.css"
      provides: "CSS custom properties (design tokens)"
      contains: "--bg: #000000"
  key_links:
    - from: "frontend/src/pages/Matches.tsx"
      to: "frontend/src/components/matches/MatchList.tsx"
      via: "import and render"
      pattern: "import.*MatchList"
    - from: "frontend/src/components/matches/OddsButton.tsx"
      to: "frontend/src/stores/slip.ts"
      via: "useSlipStore hook"
      pattern: "useSlipStore"
    - from: "frontend/src/App.tsx"
      to: "react-router"
      via: "BrowserRouter + Routes"
      pattern: "BrowserRouter|Routes"
---

<objective>
Scaffold the Vite + React + TypeScript SPA, extract the terminal UI system from the prototype (index.html), and build the match browser with bet slip.

Purpose: Create the frontend foundation that all other plans build on -- the terminal aesthetic IS the product differentiator.
Output: A working SPA with match browsing, group filtering, odds selection, and bet slip, using the exact same visual design as the prototype but with proper React components, TypeScript types, CSS Modules, Zustand state, and React Router navigation.
</objective>

<execution_context>
@C:/Users/nofun/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nofun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-commerce-foundation/01-RESEARCH.md

Source files to migrate from:
@index.html (prototype with all CSS, components, data, and interaction patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with terminal design system</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/vitest.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/styles/tokens.css
    frontend/src/styles/global.css
    frontend/src/test/setup.ts
    frontend/src/components/layout/Line.tsx
    frontend/src/components/layout/Line.module.css
    frontend/src/components/layout/Nav.tsx
    frontend/src/components/layout/Nav.module.css
    frontend/src/components/layout/Editor.tsx
    frontend/src/components/layout/Editor.module.css
    frontend/src/data/matches.ts
    frontend/src/data/groups.ts
  </files>
  <read_first>
    C:/Users/nofun/tenplusone/index.html (prototype -- extract CSS tokens, component patterns, data arrays, responsive rules)
  </read_first>
  <action>
    1. Run `npm create vite@latest frontend -- --template react-ts` from the project root (C:/Users/nofun/tenplusone/).
    2. `cd frontend && npm install react-router zustand && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`
    3. Delete the Vite boilerplate files: src/App.css, src/index.css, src/assets/react.svg, public/vite.svg.

    4. Create `frontend/src/styles/tokens.css` -- extract the exact :root vars from prototype:
       ```css
       :root {
         --bg: #000000;
         --white: #a0a0a0;
         --bright: #ffffff;
         --dim: #555555;
         --faint: #222222;
         --line-num: #333333;
         --green: #4ade80;
         --font: 'Geist Mono', 'SF Mono', 'Menlo', 'Consolas', monospace;
         --font-size: 14px;
         --line-height: 1.85;
         --letter-spacing: 0.01em;
       }
       ```

    5. Create `frontend/src/styles/global.css` -- extract base styles from prototype:
       - `@import url('https://fonts.googleapis.com/css2?family=Geist+Mono:wght@300;400;500;600;700;800;900&display=swap');`
       - `@import './tokens.css';`
       - Reset: `*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }`
       - `html, body { background: var(--bg); color: var(--white); }`
       - Body: `font-family: var(--font); font-size: 14px; line-height: 1.85; -webkit-font-smoothing: antialiased; letter-spacing: 0.01em;`
       - Selection: `::selection { background: var(--bright); color: var(--bg); }`
       - Text utility classes: `.comment { color: var(--dim); }`, `.bold { color: var(--bright); font-weight: 700; }`, `.bright { color: var(--bright); }`, `.dim { color: var(--dim); }`, `.faint { color: var(--faint); }`
       - Toast styles from prototype (`.toast`, `.toast.on`)

    6. Create `frontend/src/components/layout/Line.tsx`:
       ```typescript
       import { useRef } from 'react';
       import styles from './Line.module.css';

       interface LineProps {
         n?: number | string;
         children: React.ReactNode;
         className?: string;
       }

       export function Line({ n, children, className = '' }: LineProps) {
         return (
           <div className={`${styles.L} ${className}`}>
             <span className={styles.Ln}>{n ?? ''}</span>
             <span className={styles.Lc}>{children}</span>
           </div>
         );
       }

       export function Blank({ n }: { n?: number | string }) {
         return <Line n={n}>&nbsp;</Line>;
       }

       export function useLineCounter(start = 1) {
         const ref = useRef(start);
         ref.current = start;
         return () => ref.current++;
       }
       ```

    7. Create `frontend/src/components/layout/Line.module.css` -- extract from prototype:
       `.L { display: flex; align-items: flex-start; min-height: 26px; padding: 0 24px; }`
       `.Ln { width: 36px; flex-shrink: 0; text-align: right; padding-right: 20px; color: var(--line-num); font-size: 14px; user-select: none; line-height: 1.85; }`
       `.Lc { flex: 1; min-width: 0; line-height: 1.85; }`
       Responsive at 560px: `.L { padding: 0 12px; } .Ln { width: 28px; padding-right: 12px; font-size: 12px; } .Lc { font-size: 12px; }`

    8. Create `frontend/src/components/layout/Nav.tsx` -- sticky nav bar matching prototype exactly:
       - Brand "10+1" on left (onClick navigates to /matches)
       - Nav links: matches, groups, outrights, merch (nav-link class)
       - Slip link on right with badge count (nav-slip class with nav-badge)
       - Grid link button (nav-grid-link class, links to /grid)
       - Use React Router `<Link>` and `useLocation()` for active state (`.active` class)
       - Import slip store for bet count: `const betCount = useSlipStore(s => s.bets.length)`

    9. Create `frontend/src/components/layout/Nav.module.css` from prototype .nav, .nav-brand, .nav-link, .nav-link:hover, .nav-link.active, .nav-slip, .nav-badge, .nav-grid-link styles. Responsive at 560px.

    10. Create `frontend/src/components/layout/Editor.tsx` -- wrapper component:
        ```typescript
        import styles from './Editor.module.css';
        export function Editor({ children }: { children: React.ReactNode }) {
          return <div className={styles.editor}>{children}</div>;
        }
        ```
        Editor.module.css: `.editor { padding: 40px 0 80px; }`

    11. Create `frontend/src/data/matches.ts` -- extract MATCHES, OUTRIGHTS arrays from prototype with TypeScript types:
        ```typescript
        export interface Match {
          id: string;
          d: string;       // date string
          grp: string;     // group letter
          h: string;       // home team
          a: string;       // away team
          t: string;       // time
          v: string;       // venue
          odds: [number, number, number]; // home, draw, away
        }
        export const MATCHES: Match[] = [ /* all 24 from prototype */ ];
        export const OUTRIGHTS = [ /* all 12 from prototype */ ];
        ```

    12. Create `frontend/src/data/groups.ts` -- extract GROUPS array with types:
        ```typescript
        export interface Group {
          id: string;
          teams: string[];
          flags: string[];
        }
        export const GROUPS: Group[] = [ /* all 12 from prototype */ ];
        ```

    13. Create `frontend/vitest.config.ts`:
        ```typescript
        import { defineConfig } from 'vitest/config';
        import react from '@vitejs/plugin-react';
        export default defineConfig({
          plugins: [react()],
          test: { environment: 'jsdom', setupFiles: './src/test/setup.ts', globals: true },
        });
        ```

    14. Create `frontend/src/test/setup.ts`:
        ```typescript
        import '@testing-library/jest-dom';
        ```

    15. Update `frontend/src/main.tsx` to import `./styles/global.css` and render `<App />`.

    16. Update `frontend/src/App.tsx` to use BrowserRouter with Routes:
        - Route "/" and "/matches" -> Matches page
        - Route "/groups" -> Groups page
        - Route "/slip" -> BetSlipPage
        - Route "/merch" -> placeholder div "Merch coming soon"
        - Route "/merch/:handle" -> placeholder div "Product detail coming soon"
        - Wrap all in `<Nav />` + `<Editor />` layout

    17. Verify `npm run dev` starts without errors, `npm run build` produces dist/.
  </action>
  <verify>
    <automated>cd C:/Users/nofun/tenplusone/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"react-router"`, `"zustand"`, `"vitest"`, `"@testing-library/react"`
    - `frontend/src/styles/tokens.css` contains `--bg: #000000` and `--green: #4ade80` and `--font: 'Geist Mono'`
    - `frontend/src/styles/global.css` contains `@import './tokens.css'` and `font-family: var(--font)`
    - `frontend/src/components/layout/Line.tsx` exports `Line`, `Blank`, `useLineCounter`
    - `frontend/src/components/layout/Nav.tsx` contains `useSlipStore` and `nav-brand` and `Link`
    - `frontend/src/data/matches.ts` contains `export const MATCHES` with 24 entries and `export interface Match`
    - `frontend/src/data/groups.ts` contains `export const GROUPS` with 12 entries
    - `frontend/src/App.tsx` contains `BrowserRouter` and `Routes` and routes for `/matches`, `/groups`, `/slip`, `/merch`
    - `frontend/dist/` directory exists after build
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Vite + React + TypeScript SPA scaffolded with terminal design system (tokens, global styles, Line/Nav/Editor components), data files extracted from prototype, React Router configured, Vitest set up, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build match browser, group view, and bet slip with Zustand state</name>
  <files>
    frontend/src/stores/slip.ts
    frontend/src/stores/matches.ts
    frontend/src/components/matches/MatchList.tsx
    frontend/src/components/matches/MatchList.module.css
    frontend/src/components/matches/OddsButton.tsx
    frontend/src/components/slip/BetSlip.tsx
    frontend/src/components/slip/BetSlip.module.css
    frontend/src/pages/Matches.tsx
    frontend/src/pages/Groups.tsx
    frontend/src/pages/BetSlipPage.tsx
  </files>
  <read_first>
    C:/Users/nofun/tenplusone/index.html (prototype -- lines 300-520 have the App component with all interaction logic: toggleBet, removeBet, setWager, grpFilter, tab rendering, match card layout, bet slip layout)
    C:/Users/nofun/tenplusone/frontend/src/data/matches.ts (types created in Task 1)
    C:/Users/nofun/tenplusone/frontend/src/data/groups.ts (types created in Task 1)
    C:/Users/nofun/tenplusone/frontend/src/components/layout/Line.tsx (Line component created in Task 1)
    C:/Users/nofun/tenplusone/frontend/src/styles/tokens.css (design tokens created in Task 1)
  </read_first>
  <action>
    1. Create `frontend/src/stores/slip.ts` -- Zustand store with persist middleware:
       ```typescript
       import { create } from 'zustand';
       import { persist } from 'zustand/middleware';

       export interface Bet {
         matchId: string;
         pick: 'home' | 'away' | 'draw';
         odds: number;
         homeTeam: string;
         awayTeam: string;
         wager: string; // merch product ID or empty
       }

       interface SlipStore {
         bets: Bet[];
         toggleBet: (bet: Omit<Bet, 'wager'>) => void;
         removeBet: (matchId: string) => void;
         setWager: (matchId: string, wager: string) => void;
         clear: () => void;
       }

       export const useSlipStore = create<SlipStore>()(
         persist(
           (set) => ({
             bets: [],
             toggleBet: (bet) => set((s) => {
               const exists = s.bets.find((b) => b.matchId === bet.matchId && b.pick === bet.pick);
               if (exists) return { bets: s.bets.filter((b) => !(b.matchId === bet.matchId && b.pick === bet.pick)) };
               return { bets: [...s.bets.filter((b) => b.matchId !== bet.matchId), { ...bet, wager: '' }] };
             }),
             removeBet: (matchId) => set((s) => ({ bets: s.bets.filter((b) => b.matchId !== matchId) })),
             setWager: (matchId, wager) => set((s) => ({
               bets: s.bets.map((b) => b.matchId === matchId ? { ...b, wager } : b),
             })),
             clear: () => set({ bets: [] }),
           }),
           { name: 'tenplusone-slip' }
         )
       );
       ```

    2. Create `frontend/src/stores/matches.ts` -- simple store for group filter state:
       ```typescript
       import { create } from 'zustand';

       interface MatchesStore {
         groupFilter: string;  // 'all' or group letter A-L
         setGroupFilter: (group: string) => void;
       }

       export const useMatchesStore = create<MatchesStore>((set) => ({
         groupFilter: 'all',
         setGroupFilter: (group) => set({ groupFilter: group }),
       }));
       ```

    3. Create `frontend/src/components/matches/OddsButton.tsx`:
       - Accepts: `matchId: string`, `pick: 'home' | 'away' | 'draw'`, `odds: number`, `label: string`, `homeTeam: string`, `awayTeam: string`
       - Uses `useSlipStore` to check if this pick is selected and to toggle
       - CSS classes from prototype: `odd-btn` base, `odd-btn picked` when selected
       - On hover: `color: var(--green)` (FRNT-02 compliance)
       - Label element: `<span className="lbl">{label}</span>` with 10px font, 0.5 opacity
       - Inline styles or CSS module matching prototype `.odd-btn`, `.odd-btn:hover`, `.odd-btn.picked`, `.odd-btn .lbl`

    4. Create `frontend/src/components/matches/MatchList.tsx`:
       - Renders filtered matches using `Line` component for every row (mimicking prototype layout)
       - Each match block: date line (comment style), match line with home/away team names (bright), venue line (dim), odds row with 3 OddsButton components (1, X, 2)
       - Group filter tabs at top: "ALL" plus A-L buttons using `useMatchesStore`
       - Team names wrapped in `<Link to={`/teams/${teamSlug}`}>` with `.team-link` class (hover green)
       - Use `useLineCounter()` hook for auto-incrementing line numbers
       - Blank lines between matches for visual spacing

    5. Create `frontend/src/components/matches/MatchList.module.css`:
       - `.oddsRow { display: inline-flex; gap: 8px; margin-top: 2px; }` (from prototype .odds-row)
       - `.oddBtn` styles from prototype `.odd-btn`, `.odd-btn:hover`, `.odd-btn.picked`
       - `.lbl` styles from prototype `.odd-btn .lbl`
       - `.teamLink` styles from prototype `.team-link`
       - `.pickItem` styles from prototype `.pick-item`
       - Group filter button styles
       - Responsive at 560px: `.oddsRow { gap: 4px; }`, `.oddBtn { padding: 2px 8px; font-size: 11px; }`

    6. Create `frontend/src/components/slip/BetSlip.tsx`:
       - Shows all bets from `useSlipStore`
       - Each bet: match teams (bright), pick label, odds value, wager select dropdown (merch items -- use MERCH data from prototype for now, will be replaced with Shopify data in Plan 02)
       - Remove button (x) per bet using `removeBet`
       - Clear all button
       - "Place predictions" CTA button (disabled placeholder for now)
       - Uses Line component throughout for terminal aesthetic
       - Wager select: `.slip-select` class from prototype

    7. Create `frontend/src/components/slip/BetSlip.module.css`:
       - `.slipSelect` from prototype `.slip-select` styles
       - `.rmBtn` from prototype `.rm-btn` styles
       - `.cmdBtn` from prototype `.cmd-btn` styles
       - `.inputLine` from prototype `.input-line` styles

    8. Create page components:
       - `frontend/src/pages/Matches.tsx` -- renders `<MatchList />`, wraps in Editor
       - `frontend/src/pages/Groups.tsx` -- renders group table using Line components with all 12 groups, 4 teams each, flags, linking to team pages
       - `frontend/src/pages/BetSlipPage.tsx` -- renders `<BetSlip />`

    9. Wire pages into App.tsx routes (update if needed).

    10. Verify the app renders correctly: `npm run dev` shows matches with odds buttons, clicking odds toggles bet slip, group filter works, slip shows selected bets.
  </action>
  <verify>
    <automated>cd C:/Users/nofun/tenplusone/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/stores/slip.ts` exports `useSlipStore` with `toggleBet`, `removeBet`, `setWager`, `clear` methods
    - `frontend/src/stores/slip.ts` contains `persist` middleware with key `'tenplusone-slip'`
    - `frontend/src/stores/matches.ts` exports `useMatchesStore` with `groupFilter` and `setGroupFilter`
    - `frontend/src/components/matches/MatchList.tsx` imports `Line` from layout and `useSlipStore` from stores
    - `frontend/src/components/matches/OddsButton.tsx` contains `useSlipStore` and `toggleBet`
    - `frontend/src/components/matches/MatchList.module.css` contains `var(--green)` only in `:hover` pseudo-class
    - `frontend/src/components/slip/BetSlip.tsx` imports `useSlipStore` and renders `removeBet` button
    - `frontend/src/pages/Matches.tsx` imports and renders `MatchList`
    - `frontend/src/pages/Groups.tsx` imports `GROUPS` from data and renders all 12 groups
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Match browser with odds buttons, group filtering, and bet slip are fully functional. Clicking an odds button adds/removes a prediction. Bet slip shows selected predictions with remove and wager selection. All interactions match the prototype behavior. Terminal aesthetic maintained with line numbers, Geist Mono, monochrome palette, green only on hover.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify terminal aesthetic and interactions</name>
  <what-built>Complete terminal-aesthetic SPA with match browser, odds selection, group filtering, bet slip, and navigation. Migrated from prototype index.html to Vite + React + TypeScript with CSS Modules and Zustand.</what-built>
  <how-to-verify>
    1. Run `cd C:/Users/nofun/tenplusone/frontend && npm run dev`
    2. Open http://localhost:5173 in browser
    3. Verify visual match to prototype:
       - Geist Mono font everywhere, 14px, line-height 1.85
       - Black background, monochrome palette (#a0a0a0 text, #ffffff bright, #555555 dim)
       - Line numbers on the left in #333333
       - Green (#4ade80) appears ONLY on hover (nav links, odds buttons, team names) -- never as default color
    4. Test match browsing: scroll through 24 matches, see dates, teams, venues, odds
    5. Test group filter: click group buttons (A-L), matches filter correctly
    6. Test odds: click an odds button, verify it toggles to "picked" state. Click again to deselect.
    7. Navigate to Slip: click slip in nav, verify selected bets appear with remove buttons
    8. Test responsive: resize to 560px width, verify layout adjusts (smaller padding, font sizes)
    9. Test nav: all links (matches, groups, slip) navigate correctly, active state shows on current page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd C:/Users/nofun/tenplusone/frontend && npm run build` succeeds
- `cd C:/Users/nofun/tenplusone/frontend && npx vitest run --reporter=verbose` passes (test infrastructure works)
- Visual inspection confirms terminal aesthetic matches prototype
- Odds button click toggles bet in slip store (verified via React DevTools or UI)
</verification>

<success_criteria>
- Vite + React + TypeScript SPA builds and runs
- Terminal aesthetic pixel-matches prototype (tokens, line numbers, nav, typography)
- 24 matches render with odds buttons, group filtering works
- Bet slip stores selections in Zustand with localStorage persistence
- React Router handles navigation between matches, groups, slip, and merch placeholder
- Responsive layout at 560px breakpoint
- Green color used exclusively on hover states
</success_criteria>

<output>
After completion, create `.planning/phases/01-commerce-foundation/01-01-SUMMARY.md`
</output>
